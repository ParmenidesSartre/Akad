# This file is intentionally invalid — missing required fields (apiVersion, kind, metadata.owner)
# Used to test contract loader error handling.
apiVersion: datacontract/v1
kind: DataContract

metadata:
  name: broken_contract
  version: "1.0.0"
  # owner is deliberately missing

dataset:
  format: parquet
  location: /tmp/test.parquet

on_breach: warn
