FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml ./
COPY dashboard/ ./dashboard/

RUN pip install --no-cache-dir fastapi "uvicorn[standard]" jinja2 httpx

EXPOSE 8501
CMD ["uvicorn", "dashboard.main:app", "--host", "0.0.0.0", "--port", "8501"]
