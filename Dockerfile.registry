FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml ./
COPY akad/ ./akad/
COPY registry/ ./registry/

RUN pip install --no-cache-dir -e ".[registry]" uvicorn

EXPOSE 8000
CMD ["uvicorn", "registry.main:app", "--host", "0.0.0.0", "--port", "8000"]
